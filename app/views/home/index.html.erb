<div class="container">
  <h2 class="subtitle"><%= "#{I18n.t('popular.stream')}" %></h2>
  <%= render partial: 'streaming', locals: { users: @users } %>
</div>
<%= render partial: 'records/all', locals: { records: Record.all } %>
